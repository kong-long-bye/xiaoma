# requirements.txt 仍然可以在 Conda 环境中使用：
# conda create -n xiaoma python=3.11
# conda activate xiaoma
# python -m pip install -r requirements.txt

joblib>=1.3
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.1
